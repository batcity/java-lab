# Var:

The var reserved type name was introduced in Java 10, this lets the compiler infer the data type of a variable based on the initialized value. var can be used in variable declarations in methods, for loops and try-with-resources. It can't be used in class Fields, Catch clauses, as method parameters or return types.