# Java Entrypoint:

The Entrypoint is the location or line of code at which a java program begins execution

The default entrypoint for a java class is:

```
public static void main(String args[])
```